spring:
  application:
    name: user-api
  datasource:
    url: jdbc:mysql://localhost:3306/user_security?useSSL=false&serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    generate-ddl: true